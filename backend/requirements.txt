fastapi>=0.110.0
uvicorn[standard]>=0.27.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.4.0
pydantic>=2.6.0
websockets>=12.0
python-multipart>=0.0.9
# scapy is OPTIONAL — required only for PCAP (live network capture) mode.
# DATASET, CICIDS, and SIMULATED modes work without it.
scapy>=2.5.0

# firebase-admin is OPTIONAL — required only to PERSIST customer accounts to
# Firebase (Firestore + Auth). Without it, accounts fall back to in-memory and
# are lost on restart. See FIREBASE_SETUP.md.
firebase-admin>=6.5.0
